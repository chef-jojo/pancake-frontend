import PageHeader, { PageHeaderProps } from 'components/PageHeader'

const MarketPageHeader: React.FC<PageHeaderProps> = (props) => {
  return <PageHeader background="gradientsCardHeader" {...props} />
}

export default MarketPageHeader
