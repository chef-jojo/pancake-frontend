import styled from '@pancakeswap/styled'
import { Flex } from '@pancakeswap/uikit'

const FlexRow = styled(Flex)`
  flex: 1;
`

FlexRow.defaultProps = { alignItems: 'center' }

export default FlexRow
